library(sf)      # here we tell R that we need the Simple Features library (sf)
library(tmap)    # for static and interactive maps
library(leaflet) # for interactive maps
library(ggplot2) # tidyverse data visualization package
#then we create an object called "precs," which is short for precincts and fill it with the data from our geojson file
precs = read_sf("data/Voting_Precincts_Based_on_2020_Census_Data.geojson")
#the "head" function tells us what is in the object and what kind of object it is
head(precs)
#let's plot our object
tm_shape(precs) +
tm_fill()
#now let's try it with some borders
tm_shape(precs) +
tm_fill() +
tm_borders()
{
"type": "FeatureCollection",
library(sf)      # here we tell R that we need the Simple Features library (sf)
library(tmap)    # for static and interactive maps
library(leaflet) # for interactive maps
library(ggplot2) # tidyverse data visualization package
#then we create an object called "precs," which is short for precincts and fill it with the data from our geojson file
precs = read_sf("data/Voting_Precincts_Based_on_2020_Census_Data.geojson")
#the "head" function tells us what is in the object and what kind of object it is
head(precs)
#let's plot our object
tm_shape(precs) +
tm_fill()
#now let's try it with some borders
tm_shape(precs) +
tm_fill() +
tm_borders()
#what about some labels?
tm_shape(precs) +
tm_fill() +
tm_borders() +
tm_text("Ward_Precinct")
#that didn't work? how do we learn more about our tm_* (tmap) functions?
?tmap
#R will let you make a map object and reuse it
woo_map1 = tm_shape(precs)+tm_fill()+tm_borders()
```{r}
library(sf)      # here we tell R that we need the Simple Features library (sf)
library(tmap)    # for static and interactive maps
library(leaflet) # for interactive maps
library(ggplot2) # tidyverse data visualization package
#then we create an object called "precs," which is short for precincts and fill it with the data from our geojson file
precs = read_sf("data/Voting_Precincts_Based_on_2020_Census_Data.geojson")
#the "head" function tells us what is in the object and what kind of object it is
head(precs)
#let's plot our object
tm_shape(precs) +
tm_fill()
#now let's try it with some borders
tm_shape(precs) +
tm_fill() +
tm_borders()
#what about some labels?
tm_shape(precs) +
tm_fill() +
tm_borders() +
tm_text("Ward_Precinct")
#that didn't work? how do we learn more about our tm_* (tmap) functions?
?tmap
#R will let you make a map object and reuse it
woo_map1 = tm_shape(precs)+tm_fill()+tm_borders()
View(woo_map1)
View(precs)
View(woo_map1)
library(sf)      # here we tell R that we need the Simple Features library (sf)
library(tmap)    # for static and interactive maps
library(leaflet) # for interactive maps
library(ggplot2) # tidyverse data visualization package
library(sf)      # here we tell R that we need the Simple Features library (sf)
library(tmap)    # for static and interactive maps
library(leaflet) # for interactive maps
library(ggplot2) # tidyverse data visualization package
#then we create an object called "precs," which is short for precincts and fill it with the data from our geojson file
precs = read_sf("data/Voting_Precincts_Based_on_2020_Census_Data.geojson")
pstations = read_sf("data/Polling_Locations.geojson")
#the "head" function tells us what is in the object and what kind of object it is
head(precs)
#let's plot our object
tm_shape(precs) +
tm_fill()
#now let's try it with some borders
tm_shape(precs) +
tm_fill() +
tm_borders()
#what about some labels?
tm_shape(precs) +
tm_fill() +
tm_borders() +
tm_text("Ward_Precinct")
#that didn't work? how do we learn more about our tm_* (tmap) functions?
?tmap
#R will let you make a map object and reuse it
woo_map1 = tm_shape(precs)+tm_fill()+tm_borders()
tm_shape(precs) +
tm_fill() +
tm_borders() +
tm_text("Ward_Precinct")
#what about some labels?
tm_shape(precs) +
tm_fill() +
tm_borders() +
tm_text("Ward_Precinct", size=1)
tm_shape(precs) +
tm_fill() +
tm_borders() +
tm_text("Ward_Precinct", size=0.5)
tm_shape(precs) +
tm_fill() +
tm_borders() +
tm_text("Ward_Precinct", size="AREA")
colors()
tm_shape(precs) +
tm_fill() +
tm_borders() +
tm_text("Ward_Precinct", size="AREA", col = "royalblue1")
tm_shape(precs) +
tm_fill() +
tm_borders() +
tm_text("Ward_Precinct", size="AREA", col = "#70c6cd")
tm_shape(precs) +
tm_fill() +
tm_borders() +
tm_text("Ward_Precinct", size="AREA", col = "#d07662")
#now let's try it with some borders
tm_shape(precs) +
tm_fill("aliceblue") +
tm_borders(col = "lightpink1")
#what about some labels?
tm_shape(precs) +
tm_fill() +
tm_borders() +
tm_text("Ward_Precinct", size="AREA", col = "#d07662")
```{r}
library(sf)      # here we tell R that we need the Simple Features library (sf)
library(tmap)    # for static and interactive maps
library(leaflet) # for interactive maps
library(ggplot2) # tidyverse data visualization package
#then we create an object called "precs," which is short for precincts and fill it with the data from our geojson file
precs = read_sf("data/Voting_Precincts_Based_on_2020_Census_Data.geojson")
#the "head" function tells us what is in the object and what kind of object it is
head(precs)
#let's plot our object
tm_shape(precs) +
tm_fill()
#now let's try it with some borders
tm_shape(precs) +
tm_fill("aliceblue") +
tm_borders(col = "lightpink1")
#what about some labels?
tm_shape(precs) +
tm_fill() +
tm_borders() +
tm_text("Ward_Precinct", size="AREA", col = "#d07662")
#that didn't work? how do we learn more about our tm_* (tmap) functions?
?tmap
#R will let you make a map object and reuse it
woo_map1 = tm_shape(precs)+tm_fill()+tm_borders()
tm_shape(precs) +
tm_fill(col = "aliceblue") +
tm_borders(col = "lightpink1")
#what about some labels?
tm_shape(precs) +
tm_fill() +
tm_borders() +
tm_text("Ward_Precinct", size="AREA", col = "#d07662")
#that didn't work? how do we learn more about our tm_* (tmap) functions?
?tmap
#R will let you make a map object and reuse it
woo_map1 = tm_shape(precs)+tm_fill()+tm_borders()
tm_shape(precs) +
tm_fill(col = "red") +
tm_borders(col = "purple") +
tm_text("Ward_Precinct", size="AREA", col = "#d07662")
tm_shape(precs) +
tm_fill(col = "red") +
tm_borders(col = "purple") +
tm_text("Ward_Precinct", size="AREA", col = "black")
tm_shape(precs) +
tm_fill(col = "navy") +
tm_borders(col = "purple") +
tm_text("Ward_Precinct", size="AREA", col = "black")
tm_shape(precs) +
tm_fill(col = "navy") +
tm_borders(col = "purple") +
tm_text("Ward_Precinct", size="AREA", col = "violetred2")
tm_shape(precs) +
tm_fill(col = "springgreen2") +
tm_borders(col = "purple") +
tm_text("Ward_Precinct", size="AREA", col = "violetred2")
woo_map1
woo_map1
woo_map1 = tm_shape(precs) +
tm_fill(col = "springgreen2") +
tm_borders(col = "purple") +
tm_text("Ward_Precinct", size="AREA", col = "violetred2")
woo_map1 = tm_shape(precs) +
tm_fill(col = "springgreen2") +
tm_borders(col = "purple") +
tm_text("Ward_Precinct", size="AREA", col = "violetred2")
woo_map1
woo_map1
polls = read_sf("data/Polling_Locations.geojson")
View(pstations)
tm_shape(polls)
tm_shape(polls)
polls = read_sf("data/Polling_Locations.geojson")
head(polls)
tm_shape(polls)
polls = read_sf("data/Polling_Locations.geojson")
tm_shape(polls) +
tm_fill()
tm_symbols(polls) +
tm_fill()
tm_shape(polls) +
tm_symbols()
polls = read_sf("data/Polling_Locations.geojson")
woo_map2 = tm_shape(polls) +
tm_symbols()
woo_map2
woo_map1+woo_map2
woo_map1+woo_map2
woo_map1+woo_map2
setwd("Z:/My Documents/GitHub/GE214_F23_RGeo")
library(sf)      # here we tell R that we need the Simple Features library (sf)
library(tmap)    # for static and interactive maps
library(leaflet) # for interactive maps
library(ggplot2) # tidyverse data visualization package
precs = read_sf("data/Voting_Precincts_Based_on_2020_Census_Data.geojson")
View(precs)
library(dplyr)
library(dplyr)
results = read.csv("data/worc_23_election_atlarge.csv")
View(results)
